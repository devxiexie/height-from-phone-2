# CPU-only install. Torch CPU wheels come from the PyTorch index (see README).
torch
torchvision
ultralytics>=8.3.0        # YOLO11 detection / segmentation / pose
transformers>=4.45.0      # Depth Anything V2 pipeline
gradio>=4.44.0            # web UI
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
